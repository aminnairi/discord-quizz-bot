version: "3"

services:
    npm:
        container_name: npm
        build:
            context: docker/npm
            dockerfile: Dockerfile
        volumes:
            - .:/home/user/app

    sh:
        container_name: npm
        build:
            context: docker/sh
            dockerfile: Dockerfile
        volumes:
            - .:/home/user/app
